# Agent 工作指南

本文档为 Agent（包括 CodeBuddy、Claude Code、Cursor、GitHub Copilot 等）在本仓库中工作提供指南。

## 核心记忆

### 项目定位

quanttide-code 是量潮知识管理体系中的软件工程模块，专注代码工程化实践的标准、方法与工具。本仓库是聚合仓库，自身不放内容，只登记一组子模块，实际改动发生在子模块中。

### 仓库结构

```text
quanttide-code/
├── apps/       # 可运行的产品与站点
├── packages/   # 可复用的库与工具
├── examples/   # 示例与实验室
├── data/       # 记录性材料
└── docs/       # 规范性文档
```

子模块清单见 [README](./README.md) 的子模块表，贡献流程见 [CONTRIBUTING](./CONTRIBUTING.md)。

## 子模块操作要点

- 子模块固定在版本指针上，默认处于游离头指针（detached HEAD），动手前先 `git switch main`；
- 提交与推送顺序为先子模块、后本仓库，本仓库只记录子模块的提交哈希；
- 本仓库的提交信息格式为 `chore: 更新 <子模块路径> 子模块引用（<变更一句话>）`；
- 新增子模块后同步更新 README.md 的子模块表与 CHANGELOG.md。

## 人机协作范式

- 最小干预：仅在用户明确请求时操作；
- 信息复用：优先使用已有文档内容；
- 维护记录：修改后同步更新 CHANGELOG.md；
- 原子提交：每次提交包含完整独立变更；
- 提交即推送：提交后默认推送到远端，除非用户明确说「只提交不推」。

## 文档格式

文档遵循《量潮科技文档格式章程》：格式克制，标题不超过三级且一级标题全文唯一，代码块标注语言，中文引号使用直角引号「」。
