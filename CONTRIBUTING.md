# 贡献指南

本仓库是量潮软件工程的聚合仓库，自身不放内容，只按目录登记一组子模块，每个子模块是一个独立仓库。因此绝大多数改动发生在子模块中，本仓库只记录它们的版本指针。

## 仓库结构

子模块按用途分入五组目录：

- `apps/`：可运行的产品与站点，如 `apps/qtcloud-code`、`apps/qtcode`；
- `packages/`：可复用的库与工具，如 `packages/quanttide-code-toolkit`；
- `examples/`：示例与实验室，如 `examples/default`；
- `data/`：记录性材料，如语境、日志、档案、报告、意图、路线图、洞察；
- `docs/`：规范性文档，如章程、工作手册、标准、教程、札记、案例集。

各子模块的仓库地址与说明见 [README](./README.md) 的子模块表。

## 获取仓库

克隆时一并初始化子模块：

```sh
git clone --recurse-submodules <仓库地址>
```

已克隆的仓库补齐子模块：

```sh
git submodule update --init --recursive
```

子模块固定在版本指针上，默认处于游离头指针（detached HEAD）状态。要在其中改动，先切到该仓库的主分支：

```sh
git switch main
```

## 修改内容

内容改动提交到对应的子模块仓库，本仓库不存放内容。子模块的提交信息使用约定式提交，描述用中文，类型以 `feat`（新增）、`docs`（文档）、`chore`（维护）、`fix`（修正）为主。

## 提交与推送

每次改动默认提交并推送，不必等待指令；仅当明确说明「只提交不推」时，才停在本地。

顺序是先子模块、后本仓库，推送同理。本仓库记录的是子模块的提交哈希，若先推送本仓库，他人克隆时会被指向远端尚不存在的提交；改动涉及多个子模块时，先推送全部子模块，再推送本仓库。

## 本仓库的提交信息

本仓库自身的改动只有指针更新，提交信息格式为：

```text
chore: 更新 <子模块路径> 子模块引用（<变更一句话>）
```

单个子模块的实例如下：

```text
chore: 更新 data/roadmap 子模块引用（架构检查融合方案重写）
```

多个子模块一并更新时可并列：

```text
chore: 更新 roadmap、gallery 与 handbook 子模块引用（手册内容迁移）
```

## 新增子模块

```sh
git submodule add <仓库地址> <路径>
```

新增后同步更新 [README](./README.md) 的子模块表与 [CHANGELOG](./CHANGELOG.md)。

## 文档格式

文档遵循《量潮科技文档格式章程》，要点为格式克制（能用段落即不用列表，能用列表即不用表格）、标题不超过三级且一级标题全文唯一、代码块必须标注语言、中文引号使用直角引号「」。面向 AI 的 `AGENTS.md` 遵循同一格式要求。
